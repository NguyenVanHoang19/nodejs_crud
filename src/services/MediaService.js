"use strict";
const { Service } = require("../../systems/services");

class MediaService extends Service {
  constructor(model) {
    super(model);
  }
}

module.exports = { MediaService };
